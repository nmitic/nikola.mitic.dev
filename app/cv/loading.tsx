import { LoadingIndicator } from "../../components/loadingIndicator/LoadingIndicator";

const Loading = () => {
  return (
    <div>
      <LoadingIndicator />
    </div>
  );
};

export default Loading;
